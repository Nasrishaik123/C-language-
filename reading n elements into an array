#include <stdio.h>
int main()
{int x[100],i,n;
printf("enter the size of an array");
scanf("%d",&n);
for(i=0;i<n;i++)
{printf("enter the element");
scanf("%d",&x[i]);
}
printf("given elements are:");
for(i=0;i<n;i++)
{
    printf("\n %d ",x[i]);
}
}
  
